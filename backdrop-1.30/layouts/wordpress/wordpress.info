type = layout
name = WordPress (Generic)
description = Generic layout for ANY WordPress theme. Works with Twenty Fourteen, Fifteen, Sixteen, Seventeen, and thousands of other WordPress themes by letting themes control their own HTML structure.
version = BACKDROP_VERSION
backdrop = 1.x

; Specify regions for this layout.
regions[header] = Header
regions[content] = Content
regions[sidebar] = Sidebar
regions[footer] = Footer

; The default region automatically receives the "Main Content" block if needed.
default region = content

; Layout CSS for flex layout of content and sidebar
stylesheets[all][] = wordpress.css

; Preview (optional - can add later)
; preview = wordpress.png
